name: busecke_etal_2021_aguadv
channels:
  - conda-forge
dependencies:
  - python=3.9
  - dask
  - distributed
  - cartopy
  - astropy
  - xarray
  - numpy
  - scipy
  - dask
  - netcdf4
  - pandas
  - matplotlib
  - cmocean
  - regionmask<0.8 # This just needs a fix in cmip6_omz (for `mask_basin`) for the final version, then I can release this.
  - xesmf
  - zarr
  - h5netcdf
  - cf_xarray
  - nc-time-axis
  - xgcm
  - pip
  # these are just upstream requirements. Make sure these are not required in the final submission
  - rechunker
  - fastprogress
  - xhistogram
  - pip:
      - git+https://github.com/jbusecke/cmip6_preprocessing.git
      - git+https://github.com/jbusecke/xarrayutils.git
      - git+https://github.com/jbusecke/cmip6_omz.git #TODO: Release versions for pub
